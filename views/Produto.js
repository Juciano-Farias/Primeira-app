import React from "react";
import { StyleSheet, View, FlatList, Text } from "react-native";

export default function Produtos() {
    return (
        <View>
            <Text>Tela de cada produto</Text>
        </View>
    );
}